        <nav class="navbar navbar-dark bg-dark">
          <a class="navbar-brand p-2" href="{{ path('app_home') }}"><i class="fas fa-snowflake"></i> Gesfluid</a>
          {% if app.user %}
          <div class="m-2 text-light text-bold">
            <i class="fas fa-user"></i> {{ app.user.userIdentifier|upper }}
          </div>
          <div class="m-2">
            <a class="btn btn-sm btn-danger" href="{{ path('app_logout') }}"><i class="fas fa-right-from-bracket"></i> Déconnexion</a>
          </div>
          {% endif %}
        </nav>
